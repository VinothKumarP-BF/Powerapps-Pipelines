name: Export Managed Solution

on:
  workflow_dispatch:

jobs:
  export:
    runs-on: ubuntu-latest

    env:
      SOLUTION_NAME: Devops_POC
      TARGET_BRANCH: Managed_Solutions

    steps:

    - name: Checkout Target Branch
      uses: actions/checkout@v4
      with:
        ref: ${{ env.TARGET_BRANCH }}
        fetch-depth: 0

    - name: Install Power Platform CLI
      uses: microsoft/powerplatform-actions/actions-install@v1

    - name: Export Managed Solution
      uses: microsoft/powerplatform-actions/export-solution@v1
      with:
        environment-url: ${{ secrets.DEV_ENV_URL }}
        tenant-id: ${{ secrets.TENANT_ID }}
        app-id: ${{ secrets.CLIENT_ID }}
        client-secret: ${{ secrets.CLIENT_SECRET }}
        solution-name: ${{ env.SOLUTION_NAME }}
        solution-output-file: managed.zip
        managed: true

    - name: Unpack Managed Solution
      uses: microsoft/powerplatform-actions/unpack-solution@v1
      with:
        solution-file: managed.zip
        solution-folder: solutions/${{ env.SOLUTION_NAME }}
        solution-type: Managed

    - name: Commit Managed Solution Source
      run: |
        git config user.name "github-actions"
        git config user.email "actions@github.com"

        git add solutions/

        git commit -m "Managed export - ${{ env.SOLUTION_NAME }}" || echo "No changes"

        git push origin $TARGET_BRANCH
